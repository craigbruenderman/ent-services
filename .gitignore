# Terraform state files
*.tfstate
*.tfstate.*backup
*.tfplan

# Terraform dependency directory
.terraform/

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Variable definition files (if sensitive data is present and not intended for version control)
# Consider using environment variables or a secrets manager instead of .tfvars for sensitive info.
# *.tfvars
# *.tfvars.json

# Crash logs
crash.log

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Provider-specific files (if applicable)
# For example, for AWS provider, you might ignore credentials if not using environment variables or IAM roles.
# .aws/credentials
.terraform.lock.hcl
